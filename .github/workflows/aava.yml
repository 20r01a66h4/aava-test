name: Test AAVA Action

on:

  workflow_dispatch:

jobs:

  test:

    runs-on: ubuntu-latest

    steps:

      - name: Checkout Repository

        uses: actions/checkout@v4

      - name: Run AAVA Action

        uses: 20r01a66h4/aava-github-action@main

        with:

          workflow-id: "13387"

          inputs: |

            {

              "brd":"Testing "

            }

          token: ${{ secrets.N }}

          base-url: "https://aava-uat.aava.ai"
 
